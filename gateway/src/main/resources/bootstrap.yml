spring:
  application:
    name: gateway
  cloud:
    config:
      enabled: true
      discovery:
        service-id: config-service
      fail-fast: true
      password: 123456
      username: user
